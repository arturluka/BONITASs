import 'dotenv/config';import readline from 'node:readline/promises';import {stdin as input,stdout as output} from 'node:process';import bcrypt from 'bcryptjs';import {db} from '../database/database.js';const rl=readline.createInterface({input,output});try{const name=(await rl.question('Nome do administrador: ')).trim(),email=(await rl.question('E-mail: ')).trim().toLowerCase(),password=await rl.question('Senha (mínimo 10 caracteres): ');if(name.length<2||!email.includes('@')||password.length<10)throw new Error('Dados inválidos. Use uma senha com pelo menos 10 caracteres.');const hash=await bcrypt.hash(password,12);db.prepare('INSERT INTO admins(name,email,password_hash) VALUES(?,?,?)').run(name,email,hash);console.log('Administrador criado com sucesso.')}catch(e){console.error(e.message);process.exitCode=1}finally{rl.close();db.close()}
